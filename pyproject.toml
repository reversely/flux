[project]
name = "flux"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "detect-secrets>=1.5.0",
    "pre-commit>=4.6.2",
]

[tool.uv.workspace]
members = [
    "server",
]
